<!DOCTYPE html>

<%import>
com.moandjiezana.tent.essayist.tent.*;
</%import>
<%frag body/>
<%args>
boolean showNav = true;
String active = null;
</%args>

<%def navItem>
<%args>
String label;
String url;
</%args>
<li <%if (active != null && active.equals(label)) || url.equals(jamonContext.getPath()) %>class="active"</%if>><a href="<% jamonContext.contextPath %>/<% url %>"><% label %></a></li>
</%def>


<html>
	<head>
		<title>Essayist</title>
		
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		
		<link rel="stylesheet" href="<% jamonContext.contextPath %>/assets/bootstrap-2.1.1/css/bootstrap.min.css" />
		<link href='http://fonts.googleapis.com/css?family=Alegreya:400italic,400,700' rel='stylesheet' type='text/css'>
		<link href='http://fonts.googleapis.com/css?family=Alegreya+SC:700' rel='stylesheet' type='text/css'>
		<style type="text/css">
			body {
				padding-top: 60px;
			}
			
			body, li {
				line-height: 25px;
			}
			
			h1, h2, h3, h4 {
				font-family: 'Alegreya SC', Georgia, serif;
			}
			
			h3 small {
				font-size: 15px;
			}
			
			.bodyText {
				font-family: 'Alegreya', Georgia, serif; font-size: 1.5em;
			}
			
			.essaySummary {
				border-bottom: 1px solid #eee;
			}
		</style>
		<link rel="stylesheet" href="<% jamonContext.contextPath %>/assets/bootstrap-2.1.1/css/bootstrap-responsive.min.css" />
		
		<!--[if lt IE 9]>
		  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
		<![endif]-->
		
		<!--script src="<% jamonContext.contextPath %>/assets/bootstrap-2.1.1/js/bootstrap.min.js"></script-->
	</head>
	
	<body>
		<div class="navbar navbar-fixed-top">
			<div class="navbar-inner">
				<div class="container">
					<a class="brand" href="<% jamonContext.contextPath() %>">Essayist <% Character.valueOf('\u03B1') %></a>
					<%if showNav %>
					<%if jamonContext.isLoggedIn() %>
					<ul class="nav">
						<& navItem: "My Feed"; "read" &>
						<& navItem: "Write"; "write" &>
						<& navItem: "Written"; Entities.getForUrl(jamonContext.getSessionProfile().getCore().getEntity()) + "/essays" &>
		        	</ul>
					<ul class="nav pull-right">
						<& navItem: "Log out"; "logout"; &>
		        	</ul>
					<%else>
					<ul class="nav pull-right">
						<& navItem: "Log in / Join with Tent"; "" &>
		        	</ul>
		     		</%if>
		     		</%if>
					<ul class="nav">
						<& navItem: "Site Feed"; "global" &>
		        	</ul>
					</ul>
				</div>
			</div>
		</div>
		
		<div class="container">
		<& body &>
		</div>
	</body>
</html>
